dependencies:
  idf:
    version: ">=5.5"

  waveshare/esp32_p4_platform:
    version: "*"
    rules:
      - if: "target in [esp32p4]"

  espressif/esp_codec_dev:
    version: ">=1.5"
    public: true
    rules:
      - if: "target in [esp32p4]"

description: ESP32-P4 ES8311 helpers for AirMix BT bridge
version: 0.0.1
